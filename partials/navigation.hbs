<nav class="nav container">
    {{#foreach navigation}}
        <a class="nav-item sidebar-nav-{{slug}} {{#if current}}active{{/if}}" href="{{url absolute="true"}}">
			{{label}}
        </a>
    {{/foreach}}
</nav>
